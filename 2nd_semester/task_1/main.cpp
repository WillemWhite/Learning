/**
 * Написать функцию, принимающую длину линии из звездочек, ее направление
 * (вертикально/горизонтально), и рисующую ее в консоли. Написать функцию, которая принимает ширину
 * и высоту, и, используя предыдущую функцию, рисует прямоугольник из звездочек.
 * Определить свою функцию в отдельной единице трансляции и вызывать ее из функции main.
 */

int main(int argc, char *argv[])
{
    return 0;
}
